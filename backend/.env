# Use SQLite for local development (no Docker required)
# DATABASE_URL=sqlite:///./api_tester.db

# Or use PostgreSQL with Docker
# DATABASE_URL=postgresql://apiuser:apipass@localhost:5432/apitester

BACKEND_PORT=8000
